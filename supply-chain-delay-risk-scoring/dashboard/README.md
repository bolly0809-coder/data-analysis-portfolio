# Dashboard assets

The trained model pickle files are excluded from the public repository.

Run the modeling notebook locally to regenerate `model.pkl` and `feature_cols.pkl` before launching the dashboard.
