# Review raw data files

The original JobPlanet review Excel file is excluded from the public repository because it contains scraped review text.

This folder keeps normalized dictionary CSV files used to explain the preprocessing structure. The executed summary outputs are included under `outputs/03_review/`.
